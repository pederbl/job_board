$(".items").append("<%= escape_javascript(render partial: "items") %>");
<% if @result[:total] > @result[:matches].length %>
  next_max_id = <%= @result[:matches].last[:doc] - 1 %>;
<% else %>
  $(".more_button").hide();
<% end %>
